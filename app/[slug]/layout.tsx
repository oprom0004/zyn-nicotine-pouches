// Layout file for dynamic product routes
// Metadata is handled in page.tsx to avoid conflicts

export default function ProductLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}